# configuration — tool-3

Tool-specific configuration goes here once tool-3 is built. Prefer reading shared
settings from `shared/constants/constants.py` (backed by the platform `.env`) and
keep only this tool's own knobs local.
