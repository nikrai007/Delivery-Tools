# dependencies — tool-3

Tool-specific third-party or vendored dependencies for tool-3 (connectors,
clients, helpers). This folder is added to `sys.path` by the factory in
`../../app.py`, so modules placed here import by plain name. Keep platform-wide
packages in `requirements.txt` instead.
